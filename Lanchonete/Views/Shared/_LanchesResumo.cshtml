@model Lanche

<div class="col-sm-12 col-md-4 col-lg-3">
    <img src="@Model.ImagemUrl" class="img-fluid  h-50" alt="Foto do lanche" />
    <h3 class="pull-righ">@Model.Preco.ToString("c")</h3>
    <h3>@Model.Nome</h3>
    <p>@Model.DescricaoCurta</p>
</div>